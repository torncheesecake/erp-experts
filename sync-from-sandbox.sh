#!/bin/bash
echo "🔄 Syncing latest changes from sandbox..."

# This copies the latest files from the sandbox to your Mac
# (We'll update this path once we have a better sync method)

echo "✅ Sync complete!"
echo "Run ./deploy.sh to publish changes."
